# Codex advisory records

Codex CLI is optional. Each successful or attempted consultation is stored under `DEC-####/` as a request, structured response, human-readable response, and non-secret metadata. Do not place credentials or private secrets in advisory packets.
